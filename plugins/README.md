# Local Pelican plugins

This folder is the local plugin path. Pelican auto-discovers plugins
installed as Python packages (see `requirements.txt`); drop any
site-specific plugin modules in this folder and add their name to the
`PLUGINS` list in `pelicanconf.py`.
